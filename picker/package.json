{
  "name": "picker",
  "version": "0.8.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build:css": "tailwindcss -i src/styles.css -o popup.css --minify",
    "watch:css": "tailwindcss -i src/styles.css -o popup.css --watch",
    "lint": "eslint .",
    "test": "node --test",
    "gen:palette": "node scripts/gen-palette.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@tailwindcss/cli": "^4.0.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "tailwindcss": "^4.0.0"
  }
}
